[project]
name = "stree"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jedi-language-server>=0.43.1",
    "pathspec>=0.12.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
